---
import { getTranslations } from "@/i18n";

interface Props {
  lang: string;
}

const { lang } = Astro.props as Props;
const t = getTranslations(lang);
const {
  home: { about },
} = t;
---

<section
  class="flex flex-col items-center justify-center space-y-4 mt-6 md:mt-12 px-6 md:px-12 w-full"
>
  <span class="text-primary text-base md:text-lg">✦ {about.title}</span>
  <span class="text-base md:text-xl text-center">{about.description}</span>
</section>
